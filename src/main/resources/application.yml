server:
  error:
    include-stacktrace: on-param

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springdojo?useSSL=false&createDatabaseIfNotExists=true
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
